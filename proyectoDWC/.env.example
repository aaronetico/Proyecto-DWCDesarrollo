# Local: proxy de Vite reenvía /api al backend (vite.config.js).
VITE_API_BASE_URL=/api
VITE_PROXY_TARGET=http://127.0.0.1:8150

# Producción (p. ej. Render): URL absoluta del API Laravel, terminando en /api
# VITE_API_BASE_URL=https://TU-SERVICIO-BACKEND.onrender.com/api
